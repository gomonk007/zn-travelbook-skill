---
name: travel-guide-once
description: 为一次具体出行生成可分享的单文件旅行攻略网页。输入目的地、天数、人数等基本信息，输出一个双击即开、可直接转发的 HTML 攻略，含封面、行程总览、景点决策卡、每日动线、必读指南与行程全貌。当用户提出"做旅行攻略""规划行程""出行计划""旅游网页""做一份行程安排""攻略网页""帮我安排这次旅行"等需求时使用。适合个人或家庭单次出行场景；不适用于多城市批量生成、账号体系、行程代订代办。
agent_created: true
---

# 一次性旅行攻略分享网站

**产出物**：**单个 HTML 文件** —— 双击即开、可直接转发、无需安装任何东西。

**定位**：不做"通用城市指南"，只做**这一次出行**的攻略。给判断与招法，**不订票、不代办**。

**版本**：v0.18（口径以本文件 + `references/` 现行内容为准）。

**怎么产出**：复制 `assets/template.html` → 填内容 → **为每张景点卡与必读指南各组生成实景图并 base64 内联** →（**用户提供了腾讯地图 Key 时**）跑 `scripts/make_route_map.py --split-days` 生成**每日行程的逐天内嵌路线图（每天一张）**；**每个用餐节点落实真实店名**（见 `content-model.md` §5） → **跑 `scripts/strip_node_ids.py` 剥离编辑痕迹** → **跑 `scripts/check_consistency.py` 自检（退出码必须为 0）** → 交付。**排版已固化，不要重新设计 CSS。**

---

## 0. 三条不可让渡的立场

1. **给材料 + 给定论，不给分数。** 禁止任何数字评分 / 百分比 / 推荐指数。
   "值得去吗"要给答案，"怎么去"要给材料。
2. **把矛盾显性化，不是把景点分类。** 行程安排是 AI 旅行助手的共性短板，本 skill 攻的就是这里。
3. **不接黑箱。** 不接任何"AI 一键生成行程"的能力，那会架空本 skill 的核心逻辑。

---

## 1. 工作流：2 轮主交互 + 定向补问

**不要一问一答挤牙膏。** 尽量一次问全。

### 问话纪律：做个精明的问问题人（Q42）

**对话只做"取舍"，不做"展开"。** 页面里反正会写的东西，**不要提前在对话里讲一遍** ——
讲完再看页面就没有新鲜感，而且会干扰用户的判断。

| 轮次 | 硬上限 | 不许做 |
|---|---|---|
| **T1 收集** | 一轮问完；**问题 ≤8 条、每条 ≤12 字** | ❌ 铺垫背景 ❌ 预告产出 ❌ 举例 ❌ 复述用户已答过的话 |
| **T2 收敛** | **行式清单**（每项一行，理由 ≤12 字）；整轮**一屏之内** | ❌ 连续 3 行以上的整段 ❌ 避坑说明正文 ❌ 店名清单 ❌ 行程细节 |

**一条可自检的判据**：**同一句话，不该在对话里出现一次、又在页面里出现一次。**
只要一段内容在页面上有位置 —— 就**只在页面上说**。

> **为什么立这条**（Q42 用户原话）："对话太长，给了太多应该在攻略里提到的对话信息，用户体感不好"。
> 收集期的目标**只有拿到 L0**，不是把攻略先口头讲一遍。

### T1 —— 一次性问全（发散）

按清单**一次性**问，能答多少答多少：

| 维度 | 内容 |
|---|---|
| **Where** | 目的地 |
| **When** | 起止时间（用于判断时节与节假日叠加，不做"近期 / 远期"分档） |
| **Who** | 人数、年龄结构、关系结构 |
| **How** | 出行方式（自驾 / 高铁 / 飞机） |
| **预算** | 大致花销区间 |
| **偏好** | 想吃 / 想看 / 忌口 / 必须去的地方 |

**缺失分级处理**：

| 级 | 内容 | 处理 |
|---|---|---|
| **L0 致命** | 目的地、天数、人数 | **必须补问**，缺了做不了 |
| **L1 显著** | 预算、偏好、出行方式 | 给默认值**并标注** |
| **L2 静默** | 其余细节 | 直接默认，不提 |

> **"信息足够"的标准 = L0 齐备。** 凑齐 L0 就推进，不要为了完美而反复追问。

### T2 —— 一次性给全（收敛）

**一次性交付决断包**（三件必须齐全，缺一件不算完成）：

1. **逐项倾向** —— 每个景点给 `必去 / 值得 / 可选 / 可跳`，**必须附一句话理由**；
2. **主推行程骨架** —— Day1 / Day2 / …，每天**一句"为什么这么排"**；
3. **战术与兜底** —— 提醒 / Plan B / 避坑点。

**全部自带默认值**，用户不需要从零回答。

### T3 —— 用户校准

用户**只改想改的**。**不动 = 默认生效**，不要逼用户逐条确认。

### T4 —— 出网页

产出单个 HTML 文件（见 §3）。

---

## 2. 内容结构：顶层目录恰好 5 项

```
┌─ 封面 cover（页首，不是目录项）
│
① 行程总览 → ② 景点 ★ → ③ 每日行程 ★ → ④ 必读指南 ★ → ⑤ 行程全貌
```

**铁律：目录给人看，分类给机器用。** 内部资源分类（景点 / 美食 / 住宿 / 交通 / 打卡）**不等于**用户可见目录。
判断标准只有一条：**"一个普通游客打开页面，会不会去点它？"**

| 序 | 目录 | 承载 |
|---|---|---|
| 1 | **行程总览** | 目的地 / 行程天数 / 同行人数 / 交通方式 / 预算（行前须知并入） |
| 2 | **景点** ★ | 景点卡池（供横向比较、改倾向） |
| 3 | **每日行程** ★ | Day1…N 动线（**每天一块内嵌路线图**）+ 收拢美食 / 住宿 / 交通 / 打卡 |
| 4 | **必读指南** ★ | 按关键要素分组的避坑条目（内部标识 `pitfalls`） |
| 5 | **行程全貌** | 一张 SVG 脑图 + 城市特点卡（**只有游玩要点口诀，不含地图**） |

> 🔧 **Q39 对调 3 / 4**：原为「③ 必读指南 → ④ 每日行程」。**跟着读者的使用顺序排** ——
> 先看「怎么走」（每日行程），再查「要注意什么」（必读指南）。
> ⚠️ **改了章节顺序，必须全库同步所有「第 N 项」编号**（N12：编号也是令牌，漏改一处，下一个 agent 就照旧号排）。

**中枢对象 = `spot` 景点卡**，两问：**`why`（卡上那句话 —— 它本身就是「值得去的理由」，**卡面不出现字段名**）** / **`tip`（小提示，卡底一行）**。
**主张占整张卡的上半，避坑只占最后一行** —— 优缺点**权重不对称**，是"军师给锦囊"的口气，**不是优缺点并列的辩论**。
附加品（`addon`）**不独立编排**，带 `anchor` 挂在景点上。

**每日行程里的「吃什么」必须写真实店名**（招牌 / 一句理由 / 与上一站距离），店名属公共资源信息、要带核验状态 —— 见 `content-model.md` §5。
**「吃什么」和景点同等重要**（Q39 用户判定：对齐小红书攻略，是刚需）—— **只挂一个"看这一带吃什么"的入口不算做到**。
**「提醒」（`.say`）与「核验状态」（`.vf`）是两个角色，不要混用** —— 见 `writing-style.md` §7。

> **完整内容模型**（四象限真义 / 避坑分组与字段 / spot 与 addon 结构 / 倾向 / 分层折叠 / 事实核实）
> → 见 **`references/content-model.md`**。**生成前读一遍。**

---

## 3. 视觉：套模板，不重写 CSS

**复制 `assets/template.html`，填内容，交付。不要重新设计 CSS。**
**交付前必须运行 `scripts/strip_node_ids.py <产物.html>`**：部分宿主环境会往 skills 目录下的 HTML 资产里注入 `data-page-node-id` 节点 ID（资产源文件里无法根除，属环境行为），产物必须剥离干净再交付。

模板已固化：左目录 5 项 + 右内容（窄屏转顶栏）、纸本中国风配色（青绿主色 / 赭金辅色 / 暖纸底）、
**字号阶梯（9 档 CSS 变量 · 正文 17px · 全篇零裸字号数值）**、块状阅读（白底 + 1px 边框 + 微投影 + 交替底色）、
**景点卡顶部实景图 + 两列网格 + 卡外两个小红书标签**、**必读指南两栏瀑布流 + 每组一张分类图（图向下渐淡）**、小红书红白按钮。

**字号与悬浮反馈都收在 `:root` 变量里**（`--fs-*` / `--sh-hover` / `--ease`）。**要改字号或浮动手感 → 改变量，不要去改单条规则**（改单条就会退回到"每处一个值"的散乱状态）。

### 景点图（每张景点卡顶部）

**默认用 AI 生成实景图**，规格见 `references/spot-images.md`：1536×1024 生成 → 裁中央横条 → 缩到宽 1200 → JPEG q78 → base64 内联，**单张 ≤160 KB**。

**画面判据只有一句**：游客站在那里，眼睛看到的是什么。**按字典查表定主体，禁用"景点名里含哪个字"的关键词启发式**（曾把「漓江竹筏·杨堤→兴坪段」判成古镇）。

**三层降级**：**L1 AI 生成（默认）→ L2 用户实拍 → L0 内置 SVG 意象图**（`<use href="#sc-a…e">`，环境无生图能力时用）。**生图是主路径，不是前置条件** —— 硬依赖生图会让 skill 换个环境就跑不起来。

### 分类图（必读指南每个分组顶部）

每组顶部一张**分类实景图**（🏛 景点 / 🍜 美食 / 🏨 住宿 / 🚇 交通 / 📸 打卡），回答"**这一类长什么样**"。

- 规格：**1536×1024 → 裁 4:1 超宽横条 → 缩到宽 1000 → JPEG q78**，**单张 ≤120 KB**。
- **画面下 1/3 必须干净** —— 它会被白色渐变盖住并压上组名。
- **渐淡是 CSS 的活**（模板 `.pfhead::after`），**不要试图在生图提示词里做渐变**。
- **空组不渲染**（连图一起省）。
- **没有生图能力时：整块删掉图，落到白底** —— **别拿 SVG 意象图凑**（分类图是氛围图，符号化之后没有意义）。

> ✅ **已定（用户裁定）：不留离线缓存，每次行程重新生成。**
> 理由：分类图要贴合**本次行程**的语境（同城不同季节 / 不同玩法，该拍的画面并不一样）；固定离线虽省一次生成，但会出现「画面和这次行程对不上」的观感损失。
> 成本**已计入 D22 总账**（分类图 ≤5 张），**不额外加预算**；额度吃紧时按四条纪律走 —— **分类图先降**。

### 生图预算：推荐景点数量就是上限（D22）

**用户裁定**：*"约束推荐景点数量，也就是 AI 生图上限。"* —— **推荐几个景点，就是把生图预算定几。**

| 项 | 上限 |
|---|---|
| **景点卡总数** | **≤ min(天数 × 3, 12)** —— 3 天 → ≤9；5 天 → ≤12；7 天 → ≤12（**硬顶**） |
| **其中「必去 + 值得」** | **≤ min(天数 × 2, 12)** —— 每天最多 2 个核心点（**行程可行性的上限**） |
| **封面** | **1 张**（不随景点数量扩张） |
| **必读指南分类图** | **≤5 张**，且**只为有内容的组生成**（空组不生成） |
| **每日路线图** | **≤天数**张，**不算 AI 生图**（走地图 API，配额按接口分项另算） |

**AI 生图总账：≤ 1 + 12 + 5 = 18 张**（单张 5–10 credits → **约 90–180 credits**）。

**四条纪律**：

1. **生成前先报账** —— 向用户报**总张数 + 预估 credits**，**不要默默开跑**。
2. **失败即降级、不重试** —— 分类图 → 落白底；景点图 → 内置 SVG 意象图。
3. **降级顺序：分类图先降**（它本来就可以不存在）**，景点图最后降**（它是决策核心）。
4. **不为凑数加景点** —— 上限由**行程可行性**决定，不是由"图能画几张"决定。

> **为什么必须套模板**：排版是**定死的资产**。实测教训 —— **每次重写 CSS 就会引出反复截图调排版，把时间全耗在"验证"上。**
> 把排版固化一次、之后每次不再碰它，是治根因，不是少截几张图。
>
> **视觉规则全文**（颜色语义 / 字号阶梯 / 间距阶梯 / 框线圆角 / 图标 / 触控 / **悬浮反馈** / 断点 / 改模板清单 + 外部依据）
> → 见 **`references/design-system.md`**。**只在改模板、用户问"为什么这么排"、或要新增元素时才读。**
>
> **景点图与分类图的画面判据 / 提示词 / 裁切压缩 / 降级链** → 见 **`references/spot-images.md`**。**生成图前读一遍。**

**验证**：默认流程**不做**自截图验证。只有①改动模板本身 ②有超长无空格文本或宽表格 ③用户反馈显示问题 时才看一眼，且只做一次
→ 见 **`references/verification.md`**。

---

## 4. 封面

**每个攻略都要有封面。** 三层，带兜底：

| 层 | 做法 | 门槛 | 相关性 |
|---|---|---|---|
| **L0 保底** | CSS 生成：城市名大字 + 主色渐变 + 几何构成 | 零依赖 | 不相关，但可控 |
| **L1 主方案** | **AI 生图**：写提示词 → 生图 → base64 内联 | 运行环境有生图能力 | ★★★★ 贴合目的地 |
| **L2 增强** | 用户投喂实拍 | 用户给图 | ★★★★★ 最打动人 |

**判定顺序**：用户给了照片 → L2；否则 → **L1 生图**；生图不可用 / 失败 → **L0 CSS**（静默降级，不报错）。

**风格锁定**：**中国风审美 × 胶片做旧（富士系）**，目标是"让人联想到真实景点照片"。
**中国风管"拍什么"**（构图 / 色彩母题 / 光线），**胶片管"怎么拍"**（颗粒 / 光晕 / 暗角 / 褪色）。
**画面必须跟出行日期走**（同一城市不同月份不是一个画面）。

**硬规则**：

1. **必须留出一整块干净区域压标题**（位置随构图定，不限于上方；不要压在高频细节上）—— 封面要压主标题与副标题。
2. **要"胶片写实感"，不要"数字写实感"** —— 靠合理瑕疵把"AI 感"转成"胶片感"；反例：高饱和、HDR、过度锐化。
3. **画面必须与该时节自洽** —— 由出行日期推导；不检索就不写具体物候。
4. **不出现真实人物面孔**；不出现商标 / 赛事标志 / 现代招牌。
5. **能写"工艺"，不能写"人"** —— ✅ 可写胶片型号（材料描述）；❌ 不写 `in the style of <在世摄影师>`。
6. `source_note` 写 **"AI 生成"**，如实标注，不假装实拍。
7. **禁止外链图片** —— 一律 base64 内联（外链会破坏"双击即开"）。

> **提示词骨架、场景模板、全部依据与被否决路线的实测留档** → 见 **`references/cover-image.md`**。

---

## 5. 地图：内嵌路线图，一天一张

**地图只做一件事：把当天走过的点位和动线画在页面里。**

| 做法 | 要 Key 吗 | 用不用 |
|---|---|---|
| **L3 · 内嵌路线图** | ✅ **自备 WebService Key**（**不需要配置任何连接器**） | ✅ **唯一的做法** —— **每日行程里每天一块**，画当天点位 + **真实道路走向** |
| 免 Key 外链 | ❌ 不要 | ⛔ **不做** —— 实测点开提示"缺少 Key 和账户"、看不到路线图，体验差（**Q39 用户裁定移除**） |
| iframe 嵌入 | ❌ 不要 | ⛔ **不做** —— 离线打开是空白 |
| 行程全貌放地图 | —— | ⛔ **不做** —— 全貌只有「脑图 + 游玩要点口诀」。**地图只在每日行程**（**Q39 用户裁定**：把地图放全貌是理解任务错误） |

**没有 Key 时怎么办**：**不出现地图位** —— 当天的动线用**文字 + 序号**表达（"A → B → C"），**不留空白框、不写"此处应有地图"**。

> ⚠️ **地图只和"WebService API"有关**（它是**裸 HTTP + 一把自备 Key**，**不是连接器**，
> **不需要在连接器面板里配置任何东西** —— 只要把 Key 交给它）。
> ★ **别和"把攻略存进腾讯地图 App"混为一谈** —— 那是 **① 指南制作 MCP**，
> 产物落在**你手机的地图账号**里，**交付的那个 HTML 里一点都看不见**；
> **"地图长在页面里"只有 WebService 的静态图这一条路**（画成一张 PNG → base64 内联，**离线也能看**）。
> **把"腾讯地图·指南制作 MCP"配好了也变不出 L3** —— 它 2.0 契约明文"服务端不计算路线"。
> 三个"腾讯地图"口子的判定表 → `references/mcp-integration.md` §1。

**为什么免 Key 只能"看"**：公共页面的内置 Key 只开了「地图展示」权限，调 WebService 一律报
「此key未开启WebserviceAPI功能」（本项目实测：`status:199`）。
⚠️ 「路线规划失败，请检查网络或 API Key 配置」**是页面里写死的 UI 文案** —— 它把"权限不足"包装成"网络问题"，**别被它带偏**。

- **景点数 < 2 的当天不生成路线图**（没有可画的动线）。
- 坐标统一 `纬度,经度`（lat 在前），**绝不猜坐标**。

**用户提供了 WebService Key 时**（是甜点，不是前提）：

**跑脚本，不要手搓**：

```bash
python scripts/make_route_map.py --probe                        # 先诊断 Key（各接口各打 1 次）
python scripts/make_route_map.py --demo                         # 离线自检解码与抽稀，不联网
python scripts/make_route_map.py --input trip.json --out route.json --split-days
```

> ★★ **五条实测硬约束**（照官方文档写会踩坑，均为本项目实测所得）：
> **① 输出像素总数 ≤ 约 160 万**（= `size`宽×`scale` × 高×`scale`），超出报 `348 参数错误` ——
> 官方文档「`size` 上限 1680*1200」**不成立**。推荐 `800*500 @ scale=2`（输出 1600×1000）。
> **② URL 长度 ≤ 约 8192 字符**，超了网关直接返 `414`（HTML，不是 JSON）—— **这条决定了一张图最多画几个点**。
> 省字符两招（实测有效）：坐标取 **5 位小数**、**`,` `|` `:` 不做百分号编码**
> （要用 `urllib` 直发，不能让 `requests` 重新编码）。**点数不要手写常数** —— 按预算反推 + **用实测 URL 长度闭环**，超了自动降点重试。
> **③ 各天必须各占一个独立 `path` 参数 —— 既不能首尾相连，也不能用 `;` 串在一起**：
> 首尾相连 → 两天之间**凭空画一条直线**（桂林实测 718 m 与 **3,024 m**，横穿市区）；
> 用 `;` 串在一个 path 里 → 服务端把整条 path 当**闭合填充多边形**画（实测灰块占 **32.7%** 画面、
> 被裁切 = 「灰色半透明三角形」，用户肉眼抓到的就是它）。**点与点之间仍用 `|`**
> （实测：点用 `;` 分隔会**一个像素都不画**）。
> **④ 描线必须「白底 + 彩线」两层**；颜色越饱和越跳（青绿 `0x2F7D5C` 实测**隐身**）。
> ⚠️ **`weight` 调不动线宽**（5 / 10 / 20 两两差 <1%）；**`markers` 别传 `color`**（传了就固定青蓝，不传才是默认红）。
> **⑤ 抽稀用 RDP（按最大偏离），不用等弧长** —— 同点数下贴合度约好一倍；
> **判据是 `max_dev_m`，不是「点数够不够」**（修前 144 m ≈ 17 px 明显斜切 → 修后 5.6 m ≈ 0.65 px）。
> 取证与全部参数 → `references/map-capability.md` §4.4、§7。

1. 脚本一条龙（`--split-days`）：**逐段算路拿真实路径 → 解 polyline → RDP 抽稀（点数由 URL 预算自动定）→ 出静态图（白底彩线两层）→ 转 JPEG → base64**，**按天各出一张，放进当日行程的 `.routewrap`**；
2. **一天一个独立 `path` 参数**（Q38 真 bug）：各天**既不能首尾相连、也不能用 `;` 串在一个 path 里**（见下方硬约束 ③）；
3. ⚠️ **诚实命名，但下沉到 `.rfoot`**：喂了真实路径点 → 写「依真实导航路径绘制，非导航级」；只连直线 → 「**点位与顺序示意**」。**图位标题只写「今日动线」四个字 —— 不许用文案复述图里画了什么**（「当天 3 个点：A → B → C」这类是元说明，Q40 用户裁定）；
4. 失败（**含 `status:121` 配额耗尽**）/ 坐标不全 / 某天不足 2 点 → **那一块不生成**，**不留空占位、不写"此处应有地图"**；**121 不要重试**（同日重试必然还是 121，白耗配额）。

> ⚠️ **Key 有了也可能调不动**：配额是**按接口分项**计的，新建应用额度可能极低 ——
> 要去[配额页](https://lbs.qq.com/dev/console/quota/account)点「**一键分配**」。
> **各接口实测清单 / polyline 真实格式 / 抽稀必需 / 降级表** → 见 **`references/map-capability.md`** §2 与 §4.4。

---

## 6. 外部能力：可选，不硬依赖

> ⚠️ **触发指针：交付前要主动提示一次地图能力，征询二选一。**当行程有“每天 2+ 个点位”、且还未提示过时，先向用户说明：本 skill 有腾讯地图调用能力，配置 Key 后能给出**更准确、更可视化的页面**（每日路线图 + 真实耗时），但需要花几分钟配置。然后二选一征询：**A. 花时间启用地图能力**（按 `references/map-capability.md` §2 承接规范走；拿到 Key 先跑 `--probe` 验证、报错一步定位）；**B. 先用无地图版本**（地图位降级为文字动线，自动省略 Key）。用户选 B 就静默降级，流程照跑、不追问第二次。
> 🗣️ **面向用户的话术约束：征询呢口不上技术词，配置环节再逐步解释。**
> 征询段和很前期的对话里，**禁止出现**这些词：`map-capability` / `MCP` / `LBS` / `WebServiceAPI` / `Key` / `probe` / 路线规划 / 端口——仅用大白话：“地图功能”“配置一下”“打开网站照着点”。
> **等用户选了 A、真进入配置环节**（开始给网址、引导按钮），才逐步引入“Key / 接口 / 配额”这类词。
> 技术词只出现在配置指引里，不污染征询对话。（与 `references/writing-style.md` “WebService → 地图接口，仅与用户确认 Key 时用技术词”一脉相承）



**判据只有一条：它能不能改变交付的那个 HTML？** 按这条线，**只有一个外部能力算数**：

| 能力 | 门槛 | 定位 | 影响产出？ |
|---|---|---|---|
| **腾讯地图 WebService API** | **需自备 Key**（申请 4 步见 `map-capability.md` §2） | **建议配置** | ✅ **是** —— 解锁 ① **内嵌路线图**（地图长在页面里）② 真实耗时（带路况 ETA / 路程矩阵 / 分钟级路况） |

> ⚠️ **别把"腾讯地图"当成一个东西。** 叫这个名字的有**三个互不相干的口子**，
> 其中**两个改变不了网页**（**指南制作 MCP** 与 **官方地图 MCP**）——
> **"我配了腾讯地图 MCP，是不是就能出路线图了？"答案是不能。**
> 三行判定表与排除理由 → `references/mcp-integration.md` §1 与 §4。
>
> ★ **两种诉求，两条路，互不替代**：**"页面里要有地图" → ③ WebService**（画成图、base64 内联）；
> **"手机地图 App 里要有行程" → ① 指南制作 MCP**。
> **配了 ① 页面里不会长出地图；配了 ③ 手机里也不会多出一条行程。**

**四条铁律**：

1. **不依赖任何外部能力也能完整运行。** 硬依赖会破坏“即装即用”（P18）。
2. **⚠️ 黑箱禁区**：任何"输入目的地就输出行程"的能力（含 MCP 的 AI 一键生成行程）**一律不接** —— 它会架空本 skill 的编排逻辑与“理由优于解释”原则。**外部能力只能承接结果，不能代做决策。**
3. **能吸收的就吸收**：免 Key、无需授权的公开能力直接编进 skill 自带；**吸收 ≠ 不会失效**，仍要带降级路径（P19 / P19′）。
4. **⚠️ 工具名以运行时 schema 为准**：任何 MCP 的**官方附带文档与记忆里的工具名都可能过时**，调用前先取规格 / 看运行时 schema，**永不照抄**。

**⚠️ 不要过度承诺**：**拥堵可解（有接口），排队无解（没有这个接口）** —— 排队只能靠景点卡的 `tip`（避坑经验）+ 时段策略。

> **三个口子的判定表 / 路况能力归属 / 三级事实链 / 黑箱禁区全文 / 指南制作 MCP 留档** → 见 **`references/mcp-integration.md`**。

---

## 7. 小红书：投喂台 + 跳转出口

**只做跳转按钮（L4），不做抓取、不做 MCP。**

| 位置 | 按钮文案（即搜索词） | 意图 |
|---|---|---|
| **景点卡外 · 下方并排（左）** | **`小红书·游玩攻略`**（搜索词仍是 `{景点名} 游玩攻略`） | 看别人怎么玩这个点（决策辅助） |
| **景点卡外 · 下方并排（右）** | **`小红书·拍照参考`**（搜索词仍是 `{景点名} 拍照参考`） | 看机位 / 构图 / 穿搭（执行辅助） |

> 🔧 **Q42 改动**：**按钮上不再写景点名** —— 景点名一长就撑成两行（实测：`四姑娘山双桥沟 游玩攻略`）。
> 固定写 **`小红书·游玩攻略` / `小红书·拍照参考`**，**单行、不换行**（模板 `.xhs` 已加 `white-space:nowrap`）。
> **搜索词不变**（仍是 `{景点名} 游玩攻略`）—— 只改给读者看的文案，点进去还是搜这个景点。

> 🔧 **Q39 改动**：两个标签从**卡内**移到**卡外下方、左右并排**（模板 `.xhsrow` 用 `grid-template-columns:1fr 1fr` 保证**不出格**）。
> 🔧 **Q40 改动**：入口文案 = **景点名 + 游玩攻略 / 拍照参考**（**展示文案 = 搜索词**）。
> 上一版第二个入口写的是「小白出片」—— 那是**我们起搜索词时的站内黑话**，用户判定 *"这种词不能出，原本是我们的制作思路"*。
> **凡是"我们内部怎么得出的"，读者面前一律不出现。**
>
> ⚠️ **连带后果（必须一起改）**：卡内**再也没有可点元素** → 景点卡本体**取消悬浮位移**，
> 只保留图 `scale(1.05)`。**位移 = 可点的承诺**，点不动却浮起来就是**假可供性**（P22）。

**硬规则**：

- 按钮**只跳转，不抓取** —— 打开的是小红书已搜好的结果页，浏览发生在用户自己的 App 里。
- **禁止**把跳转结果回流进攻略内容（那就是变相抓取）。
- 样式用**红白元素**（白底红字 + 品牌红点），**不要黑底**（黑色药丸会与"必读"暗红点抢注意力）。

> **为什么不做抓取 / 不做小红书 MCP 的判据** → 见 `references/content-model.md` 与监督文档的对应条目；
> 核心一句话：**"留口"本身不等于"该留"** —— 看收益是否真实存在。

---

## 8. 执行检查清单

产出前逐项自查：

**流程与结构**

- [ ] L0（目的地 / 天数 / 人数）齐备
- [ ] 顶层目录**恰好 5 项**，顺序为**行程总览 / 景点 / 每日行程 / 必读指南 / 行程全貌**（第 5 项名字不得换成"脑图"）
- [ ] 每个景点都有**倾向 + 一句话**（`why` ≤26 字）；**卡面不出现字段名**
- [ ] 每个景点卡的 `tip` 里**含"去了可能失望的点"**
- [ ] **景点三组（必去 / 值得 / 可选）都进了两列网格**；必去卡内容**最多比值得卡多 3 行**
- [ ] 两个小红书标签在**卡外下方、左右并排**
- [ ] 附加品都带了 `anchor`
- [ ] `可跳` 项**列出但默认折叠**
- [ ] 全篇**没有任何数字评分**

**内容**

- [ ] 行程总览的小节标题是**「安排思路」**，内容**分行 2–3 条、合计 ≤120 字**（**不是一整段话**）
- [ ] 每日行程的餐饮节点**没有额外圆点标记** —— 直接写「午餐 · 店名」

- [ ] 行程总览的**「天气」卡恰好三行**（白天 / 夜间 / 天气总结），标题是「天气」，**没有第 4 行小字**
- [ ] **必带一物一行、分人群一人一行**（`.bring` 列表，不是靠逗号断句）
- [ ] 必读指南**按关键要素分组（景点 / 美食 / 住宿 / 交通 / 打卡）**，五组**两栏瀑布流**各自顶到上方（不用 `grid` 拉等高）
- [ ] 必读指南每条**只在标题前放一个 8px 点**：`hard`「必读」暗红 / `soft`「提示」暗黄 —— **不再用文字徽标**；`soft` 收进 `details`，**一律默认收起**（"无硬项就展开"的例外已废）
- [ ] 每个 `details` 的摘要文案与初始状态一致（写「提示 N 条 · 点击展开」就**必须是收起态**）
- [ ] **「提醒」（`.say`）与「核验行」（`.vf`）没有混用** —— **估算类信息给提醒；只有事实类才给核验行**
- [ ] **核验行只有一种形态**：`✅ 已核验（基于{腾讯地图 / 官方渠道 / 公开信息}）` —— **"未核实"不上页面**（不写来源、不写未核实、不写日期），由页脚一句兜底说明
- [ ] 票价 / 时间 / 预约类事实**做了时效性核验**（优先取最近通知）
- [ ] **公共资源信息**的依据**只到"大类"**（不写具体渠道名、不写日期）；**UGC 出处没有出现**在页面上

**视觉与产出**

- [ ] **套用了 `assets/template.html`**，没有自己重写 CSS
- [ ] 结构标签是**书面语**（目的地 / 行程天数 / 同行人数 / 交通方式 / 行程全貌）
- [ ] 有**封面**，且是 AI 生图或用户实拍 —— **不是空封面**（生图失败已降级到 CSS）
- [ ] 生图提示词**留出了一整块干净区域压标题**、用了**中国风构图 + 胶片做旧质感**、**画面与出行时节自洽**
- [ ] **每张景点卡都有实景图**（`可跳`档除外），且**画面与景点名对得上** —— 竹筏不配古镇、溶洞不配山景
- [ ] **景点卡数量在上限内**：总数 ≤ min(天数 × 3, 12)，且「必去 + 值得」≤ min(天数 × 2, 12)
- [ ] **生图前已报账**：总张数（封面 1 + 景点图 ≤12 + 分类图 ≤5 = **≤18**）+ 预估 credits 已告知用户
- [ ] 景点图**已裁切压缩并 base64 内联**：单张 ≤160 KB、整页 ≤3 MB、**零外链**
- [ ] `source_note` 写的是"AI 生成"（用户实拍则写"用户提供"）
- [ ] 小红书按钮在**卡外**，文案是 **`小红书·游玩攻略` / `小红书·拍照参考`**（**单行、不含景点名、不出现「小白出片」等站内黑话**），样式为**红白元素**
- [ ] **每个用餐节点都写了真实店名（带分店名）**，说明给的是 **推荐菜 + 一句理由 + 与上一站的距离 / 关系**；**没有门牌地址**；店名带 `verify` + `source`（`content-model.md` §5）
- [ ] 地图：**只在每日行程**，**全貌没有地图**；**没有免 Key 外链**；坐标**无猜测**；**没有承诺"路线规划可用"**；**图位标题只写「今日动线」**
- [ ] 用了 Key 生成内嵌地图时：跑的是 `scripts/make_route_map.py --split-days`、**每天一张**、**各天各自一个 `path` 参数**、图**已 base64 内联**、**命名诚实**（真实路径 → 加"非导航级"）、**看过 `max_dev_m`**（>60 m 说明画出来不贴路，宁可少画一天也别交付歪线）
- [ ] **产物里没有 Key**（只有图）—— Key 只存工作区 `.workbuddy/` 或环境变量
- [ ] **没有承诺能算"排队"**
- [ ] 交付物是**单个 HTML 文件**，双击即开，**图片已 base64 内联**
- [ ] 已运行 `scripts/strip_node_ids.py`，产物**不含 `data-page-node-id`** 等编辑痕迹属性
- [ ] 无个人隐私残留

**语言**

- [ ] 结构标签用书面语、体验表达保持口语（见 `references/writing-style.md`）
- [ ] 同一概念全篇用同一个词（见术语表）
- [ ] **口诀用的是常用词，没有自造缩写**（单字简称如"地 / 店 / 爹妈"一律不许；见 `writing-style.md` §5 缩写对照表）

---

## 9. 参考文件

按需读取，不要一次性全读。

| 文件 | 什么时候读 | 内容 |
|---|---|---|
| `references/content-model.md` | **生成前必读** | 顶层目录 / 四象限 / 避坑分组与字段 / spot 与 addon / 倾向 / 分层折叠 / 事实核实 |
| `references/design-system.md` | 改模板、被问"为什么这么排"、新增元素时 | 颜色语义 / **字号阶梯（9 档变量）** / 间距阶梯 / 框线圆角 / 图标 / 触控 / **悬浮反馈** / 断点 / 改模板清单 + 外部依据 |
| `references/cover-image.md` | 需要生成封面时 | 提示词骨架 / 场景模板 / 保底与降级 / 被否决路线的实测留档 |
| `references/spot-images.md` | **生成景点图 / 分类图前** | 画面判据字典 / 提示词 / 裁切压缩 / 体积预算 / 降级链 / **§9 分类图** |
| `references/map-capability.md` | 需要生成地图时 | **每日行程逐天一张内嵌路线图** / 坐标规范 / **Key 申请与 L3** / **`place/v1/search` 拿店名** / 降级 |
| `references/mcp-integration.md` | 用户要接外部能力、或问“腾讯地图到底该用哪个”时 | **§1 三个口子判定表** / 路况能力归属 / 三级事实链 / 黑箱禁区 / **§5 指南制作 MCP 留档** |
| `references/verification.md` | 命中三种例外时 | 何时验证 / 无头截图命令 / 三条实测坑 |
| `references/writing-style.md` | 写文案、校对用词时 | 书面语与口语的分界 / **七类语言问题** / **口诀缩写对照表** / 术语表 |
| **`assets/template.html`** | **每次生成（必用）** | **HTML 骨架 + 固化排版，复制套用，不要重写 CSS** |
| `scripts/make_route_map.py` | **有腾讯地图 Key、要出内嵌路线图时** | 诊断 `--probe` / 离线自检 `--demo` / 出图；polyline 解码 + 抽稀 + JPEG 压缩 + 失败降级 |
| `scripts/example_trip.json` | 同上 | 输入样例（桂林 3 天），照它的格式写 `trip.json` |
| `scripts/strip_node_ids.py` | **每次交付前（必跑）** | 剥离宿主注入的 `data-page-node-id` 编辑痕迹属性 |
| `scripts/check_consistency.py` | **每次交付前（必跑）** | **规则自检器**：过时令牌 / 表格列数 / 顶层目录链顺序 / 模板（零裸字号、无过时 class、结构件齐备、TOC↔section 对齐）。**退出码 0 = 通过**。改了口径要把旧词加进它顶部的 `TOKENS` 表，否则等于没有自检 |
